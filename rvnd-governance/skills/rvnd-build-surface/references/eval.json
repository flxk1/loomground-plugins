{
  "skill": "rvnd-build-surface",
  "provides": "rvnd.build-surface",
  "drives": "none - local deterministic linter over the plugin's apps/ card specs and schemas/",
  "wraps_kernel": false,
  "scripted": true,
  "script": "scripts/lint_surface.py",
  "network": false,
  "deterministic": true,
  "fail_closed": true,
  "loomground_language": "0.8.0a1",
  "manifest": "manifest.yaml",
  "lints": ["surface card", "composition manifest", "proposal envelope"],
  "enforces": [
    "a composition rendering 'proposal' must also render 'patch' and 'receipt'",
    "forbids_scores and attributed must be true (discrete lamps, attributed-not-asserted)",
    "proposal status_vocabulary excludes granted/enabled/active/in-effect",
    "every composition is fail_closed and drives the rvnd-governance server",
    "a proposal envelope carries a residual ledger, records its language version, and its constructs are restricted to the real 0.8.0a1 vocabulary",
    "card selection follows present construct types; a residual panel shows whenever residual exists"
  ],
  "provenance": "Authored for loomground; validates RVND surface and proposal specs against the plugin schemas. No third-party skill source.",
  "notes": "Progressive-disclosure: lean SKILL.md + references/ pair + shared plugin references. Uses optional jsonschema when present; plain checks always run. Commercial (RVND) tier of the open-core split.",
  "tests": ["tests/test_rvnd_surface.py"],
  "review": "pending Codex review"
}
