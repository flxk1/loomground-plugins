# loomground-plugins

> Distribute Loomground skills and one signed runtime to Claude, Codex/OpenAI, Cursor, n8n, and generic MCP/Agent Skills hosts.

## Problem
Without one catalogue, host packages drift from the skills and versions maintained beside each Loomground tool.

## Install
Claude Code:

```text
/plugin marketplace add flxk1/loomground-plugins
/plugin install loomground-suite@loomground
```

Runtime and host adapters:

```bash
loomground runtime verify /path/to/runtime-bundle --public-key /path/to/release-public.pem
loomground runtime install /path/to/runtime-bundle --public-key /path/to/release-public.pem --destination /absolute/runtime
loomground adapter --host <local-host> --runtime-destination /absolute/runtime
loomground adapter --host openai --server-url https://loomground.example/mcp
loomground adapter --host n8n --server-url https://loomground.example/sse
loomground onboard  # guided runtime install plus non-mutating host/maker handoff
```

`<local-host>` is one of `claude`, `codex`, `cursor`, or `generic`. The adapter command renders only; host configuration and credentials remain host-owned.

`onboard` verifies and installs one already-obtained runtime bundle, then writes a new reviewable onboarding directory with per-host merge files and optional Maker declarations. It never edits host configuration, stores credentials, or converts a Maker declaration into enforcement. Hard enforcement requires the Loomground adapter to own the Maker's only effect path.

## Usage
`externals.json` pins 13 public source repositories by commit. Five sources carry a `package.json`; eight sources carry a committed Claude plugin manifest. `tools/build_packages.py` validates those sources and builds host-specific packages. `.claude-plugin/marketplace.json` is the committed Claude Code catalogue.

The catalogue distributes 25 skills across governance, runtime controls, evidence, ingest, solver analysis, Versum knowledge work, and the language planes.

## Contracts
| Surface | Contract |
| --- | --- |
| source registry | `externals.json`: repository URL, immutable commit, sibling path |
| package manifest | `<source>/package.json`, checked against `schemas/loomground-package.schema.json` |
| Claude catalogue | `.claude-plugin/marketplace.json`, regenerated from the pinned sources |
| suite | `plugins/loomground-suite`: control skill, MCP registration, action/receipt schemas |
| runtime | DSSE/Ed25519-signed lock plus exact offline wheel set; transactional target install |
| host adapters | pure renderers for Claude, Codex, Cursor, OpenAI, n8n and generic MCP |
| onboarding | confirmed runtime install plus atomic, non-secret host and Maker handoff pack |
| runtime release | six-platform Python 3.12 matrix; ephemeral Ed25519 keys; GitHub OIDC/Sigstore provenance |
| ecosystem certification | all 41 public repositories at exact revisions plus eight cross-repository fail-closed scenarios |
| release gate | JSON validity, supply-chain checks, generated-catalogue parity, tests |

## Ecosystem certification
`ecosystem/manifest.json` is the canonical test inventory. Validate it with
`python tools/ecosystem_certify.py`. Aggregate a complete directory of signed or
attested repository and scenario results with `--results`; certification fails
when any result is missing, stale, failed, duplicated, or digest-invalid. Details:
`docs/ECOSYSTEM-TESTING.md`.

The manual `ecosystem-repository-evidence` workflow requires exactly one passing,
revision-bound result for all 41 repositories. It consumes native GitHub checks
where available and runs isolated native tests for the four historical pins that
have no check-run evidence. It also verifies and installs the published signed
runtime, checks its 32 Git pins, executes all eight scenarios through MCP, binds
each trace to its contract and participant revisions, and emits one independently
verified 41+8 certificate. This certifies the tested pins and scenarios, not every
possible deployment or an effect path the Loomground adapter does not own.

## Family
Interfaces and authoring. Consumes committed skills from 13 Loomground repositories at immutable pins. Produces installation packages for supported skill hosts. Plane execution remains in `loomground-mcp` and the source repositories.

Language: loomground — guide https://github.com/flxk1/loomground/blob/main/llms.txt (the .lg language an agent reads, emits, validates); catalogue https://github.com/flxk1/loomground/blob/main/CATALOGUE.json
